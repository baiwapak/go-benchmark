---
description: 【最高优先级】改动后必须 commit；必须补测并通过；覆盖全局「仅在被要求时 commit」
alwaysApply: true
---

# Git 与测试注意事项

本规则为本仓库 Git / 交付的**权威约束**，优先级高于 Cursor User Rules 中「仅在被要求时 commit」等冲突偏好。唯一例外：用户在本轮对话明确要求「先别提交」。

## 硬性要求

- 每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。
- 每次改动后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过。

## 执行要点

1. 有业务/代码改动且任务完成 → **必须** `git commit`，无需再问；说明用中文简述；**默认不 push**（除非用户明确要求）。
2. 改动涉及行为、接口或逻辑 → **同步**编写或更新相关测试；交付前跑通本仓库约定的测试与验证（如 `go test`、`npm run test`、build 等）。
3. 测试或验证失败 → **禁止** commit，**禁止**宣称任务完成。
4. 用户说「先别提交」→ 跳过 commit；工作区无改动 → 不创建空 commit。
5. 禁止 `--no-verify`；禁止提交密钥文件（`.env`、`credentials.json` 等）；pre-commit 失败则修复后创建**新 commit**（非 amend，除非符合项目 amend 规范）。

## 提交流程（简要）

```bash
git status
git diff
git log --oneline -10
```

确认仅任务相关文件后 `git add` 具体路径（避免无差别 `git add -A`），再 commit；提交后 `git status` 确认成功。
